# Development Profile Configuration
spring.application.name=BladeCommerce

# MongoDB - Use cloud MongoDB Atlas (same as production)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://kesikbicakcilik31:Arfmt123.@knifecluster.ugoj8rz.mongodb.net/?retryWrites=true&w=majority&appName=KnifeCluster}
spring.data.mongodb.database=${MONGODB_DATABASE:knife_commerce}

# Server Configuration
server.port=${SERVER_PORT:8080}
server.tomcat.max-threads=${SERVER_MAX_THREADS:300}

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging - Verbose for development
logging.file.name=logs/application-dev.log
logging.level.root=INFO
logging.level.com.kesik.bladecommerce=DEBUG
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=10

# File upload size limits
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# Cloudinary Configuration - Optional for dev, will use dummy values if not set
cloudinary.cloud.name=${CLOUDINARY_CLOUD_NAME:dqgnkfvrz}
cloudinary.api.key=${CLOUDINARY_API_KEY:dummy-key-for-dev}
cloudinary.api.secret=${CLOUDINARY_API_SECRET:dummy-secret-for-dev}

# JWT Configuration - Use strong secret in production
jwt.secret=${JWT_SECRET:MySecretKeyForJWTTokenGenerationAndValidationMinimum256Bits}
jwt.expiration=${JWT_EXPIRATION_MS:86400000}

# CORS - Allow localhost for development
cors.allowed.origins=http://localhost:3000,http://localhost:3001

# Enable DevTools
spring.devtools.restart.enabled=true
